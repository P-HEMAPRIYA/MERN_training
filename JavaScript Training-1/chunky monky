const chunkArray=(arr, size)=> {
    const chunks = [];
    let i = 0;
    while (i < arr.length) {
      chunks.push(arr.slice(i, i + size));
      i += size;
    }
    return chunks;
  }
  
  console.log(chunkArray([1, 2, 3, 4, 5], 2));
  
